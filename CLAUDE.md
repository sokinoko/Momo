# 윤리와 사상 노트 — Claude Code 안내

형석(고3, 수능 윤리와 사상)이 쓰는 학습 웹앱. 폰 홈 화면에 붙여 쓰는 **단일 HTML**이다.
응답은 한국어, 반말로 짧게. 결과는 "무엇이 바뀌었나"부터 말한다.
자세한 역사·데이터 현황은 `docs/인수인계.md` (필요할 때만 읽기).

## 구조
- `app.js` 엔진(상태·OX·제시문·모의고사) / `views.js` 화면 / `style.css`
- `topics_all.json` 필기 37주제 · `ox_items.json` 선지 · `passages.json` 제시문
- `build.py` → 위 전부를 `ethics_note.html` 한 파일로 묶는다
- `tests/test_mock.js` 모의고사 회귀 테스트 · `src_2027_9/ps_add.py` 제시문 추가 모듈

## 명령
```bash
python3 build.py                 # ethics_note.html (커밋하지 않음)
for t in tests/test_*.js; do node "$t" | tail -1; done
```
변경 후에는 **반드시 테스트와 빌드를 둘 다 통과**시키고 푸시한다.
화면을 바꿨다면 가능하면 Playwright로 390×844 스크린샷을 찍어 확인한다.

## 배포
`main`에 들어가면 GitHub Actions가 테스트 → 빌드 → GitHub Pages 배포를 한다.
빌드 결과물(html)은 저장소에 넣지 않는다. 작업은 브랜치에서 하고 PR로 올린다.

## 절대 규칙
1. 정답은 타이핑된 「빠른 정답」에서만 옮긴다. 손글씨 채점본 금지. 개수 재검산.
2. 본문을 지어내지 않는다. 사진이 잘리면 그 번호는 빼고 보고한다.
3. id를 추측하지 않는다. 실제 데이터를 먼저 읽는다.
4. 중복 판정은 본문 + 출처가 둘 다 같을 때만.
5. 데이터는 json을 고치고 `build.py`를 다시 돌린다. html 직접 수정 금지.
6. 필기는 주제를 나누지 않는다(`OX_TOPIC_EXTRA` / `OX_TOPIC_CHILDREN` 사용).
7. 화면에 교재 이름(포스텝·심기일전)을 노출하지 않는다.
8. 해설을 정답에 끼워 맞추지 않는다. 근거가 없으면 확인 요청으로 돌린다.

데이터 입력 스크립트에는 이 검증을 넣는다: 없는 id / fix는 X선지에만 / 이미 작성됨 /
제시문 단서는 본문에 그대로 / same은 실제 선지이면서 자기 자신이 아님.
제시문은 `ps_add.add(..., dry=True)`로 먼저 검사한다. 새 이름을 만들기 전에 이름 갈림을 확인한다.
본문에 줄바꿈 금지, 곧은 따옴표만.

## 모의고사 (app.js 「모의고사」 구간 · views.js `renderMock*`)
- **선지는 그 라벨이 가리키는 사상가 자신의 O/X로만 판정한다.** 라벨 없이 다른 사람 선지를 섞지 않는다.
- 벤다이어그램·순서도는 「갑은 을과 달리」 양방향 + 「모두」 O선지가 있는 쌍(`pool.venn`)에서만.
- 문항 유형은 여덟이다 — 단독(옳은/옳지 않은) · 갑을 · 보기 · 2중 벤 · 순서도 ·
  갑·을·병 · **3중 벤** · **서로에 대한 비판**.
- 출제기가 아는 사상가 이름은 `passages.json`에 있는 것뿐이다. 기출이 「양명」이라 불러도
  제시문 이름이 「왕수인」이면 「양명은 ~」 선지는 파싱되지 않는다. 새 이름을 쓰기 전에 확인한다.
- **시험지 구성**: 개수를 고정하지 않는다. `buildMockQuota`가 세트마다 `MOCK_QUOTA`
  범위 안에서 뽑는다 — 순서도 2~3(두 문항 이상은 지킨다), 벤다이어그램(2중이든 3중이든) 1~2.
  무작위로 채우는
  자리는 그 수를 넘지 못한다. 벤은 3중부터 볼지 2중부터 볼지도 세트마다 뽑는다
  (`MOCK_TRIO_VENN_RATE`). 늘 3중을 먼저 보면 2중이 시험지에서 사라진다.
  테스트가 세트마다 범위를 확인한다.
- **그림 문항(순서도·벤·비판)은 몰아넣지 않는다.** 다 만든 뒤 `mockSpread`가 고르게 흩고
  유형 순서도 섞는다. 붙어 있으면(한 칸도 안 띄우면) 테스트가 잡는다. 자리만 바꾸므로
  뽑히는 문제는 그대로다 — rng를 맨 마지막에 쓴다.
- **삼중 판정 선지는 사람만 다르고 문장이 같다.** 그래서 두 사람 선지를 섞는 출제기는
  모두 `mockPlainBody`로 본문 중복을 걸러야 한다(box · pair · trio · venn).
- 3중 벤 재료는 `mockTrioSets`가 모든 선지에서 모은다 — 단독 선지 하나,
  「A와 B는 모두 P」 O선지 둘, 「A는 B와 달리 P」 O선지 둘(뒷사람은 X). 본문이 글자 그대로
  같아야 한 문장으로 합쳐지고, 판정이 엇갈리는 문장은 통째로 버린다. 그래서 같은 문장에
  사람을 더 붙일수록 쓸 수 있는 조합이 늘어난다. 비판 문항은 `crit`이 달린 문장만 쓴다.
  평가원 3중 벤은 일곱 영역(갑만·을만·병만·갑을·을병·갑병·모두)을 두고 그중 넷을
  골라 A~D로 이름 붙인다. **가운데(셋 모두)는 반드시 배당된다.** 그래서 「모두」가 없는
  조합은 못 쓰고, 나머지 자리가 많을수록 뽑을 수 있는 문항 모양이 늘어난다.
  한 문장을 세 사람에게 각각 O/X로 매겨 두면 어느 영역인지 저절로 정해진다.
  `tests/test_trio_venn.js`가 조합별로 영역 채움과 문항 모양 수를 센다.
- 제한 시간 30분, 채점은 **OMR(`run.omr`)** 기준. 시험지 탭은 연필 표시(`run.picks`).
  답은 항상 `mockAns(run, i)`로 읽는다(옛 기록 호환).
- OMR 서랍은 `body` 바로 아래 `#omrLayer`에 그린다. `#screen` 안으로 옮기지 않는다(폰에서 안 뜸).
- 확인 창(`confirm`)은 쓰지 않는다. 홈 화면 앱에서 막힌다.
- 같은 날·같은 세트 번호면 같은 문제. 유형별 표본 수가 바뀌면 출제기를 건드린 것이니 이유를 설명한다.
- `let`/`const` 전역을 테스트에서 쓰려면 `tests/test_mock.js`의 eval 치환 목록에 추가한다.
- 자체 제작 비교 선지는 `cmp_items.json`(모의고사 전용, 기출 OX 탭에 안 나옴).
  `src_2027_9/cmp_add.py`로 넣는다. 해설 필수. 원전에서 딴 것은 `psid`(제시문 id)와
  `quote`(제시문 본문에 그대로 있는 구절)를 달면 채점 해설에 원전이 함께 뜬다.
- 비유·사례는 선지 본문으로 쓰지 않는다(굽은 나무, 빈 그릇, 우물에 빠진 아이).
  그 비유가 말하는 주장으로 바꿔 쓰고 비유 자체는 해설에 남긴다. 평가원도 그렇게 낸다.
- 기출이 아닌 선지는 채점 해설에 「자체 제작」 딱지가 붙는다(`mockMineTag`). 시험지에는 안 붙인다.

## 글꼴
- 모의고사: 발문·선지 나눔고딕 `--font-exam`, 제시문 나눔명조 `--font-exam-ps`
- 기출 OX 카드: 나눔고딕 / 필기(`.outline` `.cmp-table` 사상가 카드): IBM Plex Sans KR `--font-note`
- 홈 첫 카드 숫자·라벨: 개구 `--font-hand`. 손글씨라 작아 보여서 다른 곳보다 크게 잡았다
- 그림 안의 한글도 개구. `kaoHtml()`이 그릴 때만 글자마다 `.kao-ko`로 감싼다 —
  `HERO_KAO` 자체는 손대지 않는다. 개구 한글이 전각보다 좁아 상자가 어긋나므로
  `.kao-ko`는 1em 칸을 유지하고 글자만 `transform:scale`로 키운다(칸을 바꾸면 그림이 깨진다)
- 흐름으로 읽기: 노토 세리프(그대로 둔다). 구글 폰트 링크는 `build.py` 머리에 있다.

## 그 밖에
- 홈 첫 카드 그림은 `views.js`의 `HERO_KAO`(JSON 문자열). 줄바꿈·전각 공백이 모양이니 손대지 않는다.
- 부록 구조도는 SVG(`svgTree` 등). CSS로 도형을 그리지 않는다.
- 학습 기록은 브라우저 저장소에 있다. 저장 키·구조를 바꾸면 옛 기록 호환 코드를 같이 넣는다.
