# 윤리와 사상 노트

수능 윤리와 사상 학습용 단일 HTML 웹앱.

- 빌드: `python3 build.py` → `ethics_note.html`
- 테스트: `node tests/test_mock.js`
- 배포: `main`에 푸시하면 GitHub Actions가 GitHub Pages로 올린다
  (저장소 Settings → Pages → Source를 **GitHub Actions**로 한 번 설정)

작업 규칙은 `CLAUDE.md`, 자세한 인수인계는 `docs/인수인계.md`.
